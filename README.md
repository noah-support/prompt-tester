# prompt-tester
A user friendly non-technical tool to edit prompts and quickly test the improvements.

# main features
- Connect to an api & copy the datamodel
- Visual prompt editor
- Visual editor for the output schema
- Generate a new output using chosen parameters
- JSON viewer of the generated data
- Export a markdown formated version of the output
- Compare output versions against each other.
